# アプリ本体
streamlit>=1.58        # st.plotly_chart(on_select=...) / st.segmented_control / st.tabs(key=...) など
requests>=2.31
pandas>=2.0
plotly>=5.24
openpyxl>=3.1
python-dateutil>=2.8
python-dotenv>=1.0
matplotlib>=3.7        # 画像一括ダウンロード（従来形式のPNG書き出し）

# zero-plotter 連携（legs_table 読み取り）・BigQuery バックエンド
google-cloud-bigquery[pandas]>=3.11

# 開発用
pytest>=8.0
