# 変更履歴

バージョンは `src/version.py` の `__version__` が単一ソース。
リリースのたびにそこを更新し、同じ番号で git タグ `vX.Y.Z` を打つ。
形式はセマンティックバージョニング（MAJOR.MINOR.PATCH）。

## v1.1.0 — 2026-08-13

### 追加
- **「CSV で指標を置き換え」が 1 つの CSV から複数の指標を同時に置き換え可能に。**
  従来は CSV 1 ファイルにつき 1 指標だったため、同じ運行の複数指標を差し替えるには
  同じ CSV を複数回アップロードする必要があった。パネルの
  「この CSV から置き換える指標の数」を増やすと、行ごとに対象指標・値列・
  scale/offset を指定して一括適用できる（適用先期間は CSV につき 1 つ）。
- 汎用 CSV 形式（`t_ns` 列なし）が「1列目=時間、2列目以降=値」の複数値列に対応
  （従来は 2 列目のみが値）。

## v1.0.1 — 2026-07-27

自動運転判定を Yatagarasu 202605a の enum 変更に追従させた修正リリース。
**202605a 以降のデータを扱う場合はこのバージョン以降が必要。**

### 修正
- **自動運転（kAutonomousDriving）の判定が 202605a 以降のデータで誤っていた問題を修正。**
  202605a で SystemState enum の番号が変わり（kAutonomousDriving 4→16、旧 4 は
  kControlOk に再割当て）、従来のハードコード `system_state = 4` では自動運転が
  取れず、手動運転として集計されていた。
- 判定を `src/domain/drive_state.py` に集約し、ラベル `kAutonomousDriving` を
  世代の対応表から値に引き直す方式へ変更。今後また番号が変わっても、この 1 ファイルに
  世代を追加するだけで全機能に反映される。
- 世代の決定は「①サイドバーの明示指定 → ②録画自身の state 実データ（値 5 以上が
  あれば新 enum と確定）→ ③運行日（2026-05-01 JST を境界）」の優先順。
- Zero-Plotter タブの状態色分けも世代対応（新録画で状態名が黒／誤色になっていた）。
- 各期間タブに判定根拠と state 秒数の内訳を表示（誤世代や手動 0 件の切り分け用）。

### 追加
- **mcap CSV による BigQuery 欠損の穴埋め**（GetMcapToCsv 連携）
  - CSV を新しい期間として取り込む「mcap CSV 期間」
  - 取得済み期間の特定指標だけを差し替える「CSV で指標を置き換え」
    （散布図/地図/表/ヒストグラム/比較に即反映。settings.json に設定を保存・再適用可）
- SystemState enum 世代の手動指定（サイドバー「開発用」）

### 注意
- 横G 等のヒストグラムの手動側は pose テーブルに行がある秒しか集計できない。
  起動中に pose 未配信の録画では、非自動の秒があっても手動が 0 件になりうる
  （Zero-Plotter タブは state テーブル自身の位置を描くため点は出る。矛盾ではない）。

## v1.0.0

- 配布用の初期バージョン。アプリバージョン表示を追加
  （ブラウザタブ名・タイトル直下・settings.json ヘッダ）。
